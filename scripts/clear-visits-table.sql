-- Скрипт для очистки таблицы visits перед применением новой миграции
-- ВНИМАНИЕ: Это удалит все существующие данные приемов!

-- Удаляем все связанные записи информированных согласий (если таблица уже существует)
-- Примечание: если таблица informed_consents еще не создана, эти команды просто не выполнятся
DELETE FROM informed_consents WHERE visit_id IS NOT NULL;

-- Удаляем все записи из таблицы visits
DELETE FROM visits;

-- Очищаем автоинкремент (опционально, для SQLite)
-- DELETE FROM sqlite_sequence WHERE name = 'visits';
