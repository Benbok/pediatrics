# Модуль Visits (Приемы)

## 📋 Общее описание

Модуль `visits` отвечает за управление медицинскими приемами пациентов в педиатрической системе. Модуль реализует полный цикл работы с приемами: создание, редактирование, просмотр истории, AI-анализ симптомов, подбор диагнозов и назначение терапии.

## 🏗️ Архитектура

Модуль следует строгой многослойной архитектуре согласно `AI_CODING_GUIDELINES.md`:

```
┌─────────────────────────────────────────────────────────┐
│  Component Layer (*.tsx)                                │
│  - UI только, NO business logic                          │
│  - Обработка пользовательского ввода                    │
│  - Отображение данных                                    │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ↓
┌─────────────────────────────────────────────────────────┐
│  Service Layer (*.service.ts)                           │
│  - ВСЯ бизнес-логика                                     │
│  - Zod валидация (первый уровень)                       │
│  - IPC вызовы к backend                                  │
│  - Обработка ошибок и логирование                        │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ↓
┌─────────────────────────────────────────────────────────┐
│  IPC Handler Layer (electron/modules/visits/*.cjs)      │
│  - ensureAuthenticated middleware                        │
│  - Zod валидация (второй уровень - безопасность)        │
│  - Простые DB операции через Prisma                     │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ↓
┌─────────────────────────────────────────────────────────┐
│  Database Layer (Prisma)                                 │
│  - Хранение данных                                       │
│  - Миграции                                              │
└─────────────────────────────────────────────────────────┘
```

### Принципы архитектуры

1. **Разделение ответственности**: Каждый слой имеет четко определенную роль
2. **Двойная валидация**: Frontend (UX) + Backend (Security)
3. **Централизованное логирование**: Все ошибки через `logger`
4. **Аутентификация**: Все IPC handlers защищены `ensureAuthenticated`

## 📁 Структура файлов

```
src/modules/visits/
├── README.md                          # Этот файл
├── VisitsModule.tsx                   # Список всех приемов пациента
├── VisitFormPage.tsx                   # Форма создания/редактирования приема
├── constants.ts                        # Константы (нормы пульса, АД)
│
├── components/                         # UI компоненты
│   ├── anamnesis025/                  # Компоненты анамнеза жизни (форма 025/у)
│   │   ├── HereditySection.tsx        # Наследственность
│   │   ├── BirthSection.tsx           # Сведения о беременности и родах
│   │   ├── FeedingSection.tsx         # Вскармливание
│   │   ├── InfectiousDiseasesSection.tsx # Перенесенные инфекции
│   │   ├── AllergyStatusSection.tsx  # Аллергический статус
│   │   └── index.tsx                  # Экспорты
│   │
│   ├── AnamnesisSection.tsx           # Обертка для анамнеза жизни 025/у
│   ├── DiseaseHistorySection.tsx      # Анамнез заболевания
│   ├── VitalSignsSection.tsx          # Показатели жизнедеятельности
│   ├── PhysicalExamBySystems.tsx      # Объективный осмотр по системам
│   ├── DiagnosisSelector.tsx         # Выбор диагноза (основной/осложнения/сопутствующий)
│   ├── MultipleDiagnosisSelector.tsx  # Множественный выбор диагнозов
│   ├── DiseaseSearchModal.tsx        # Поиск заболевания в базе знаний
│   ├── DiseaseSidePanel.tsx           # Просмотр информации о заболевании
│   ├── IcdCodeSearchModal.tsx         # Поиск МКБ-10 кодов
│   ├── MedicationBrowser.tsx         # Браузер препаратов
│   ├── MedicationSelector.tsx         # Выбор препарата для назначения
│   ├── MedicationDoseModal.tsx       # Расчет дозировки препарата
│   ├── MedicationTemplateSelector.tsx # Выбор шаблона назначений
│   ├── MedicationTemplateBatchEditor.tsx # Пакетное редактирование назначений
│   ├── CreateMedicationTemplateModal.tsx # Создание шаблона назначений
│   ├── VisitTemplateSelector.tsx      # Выбор шаблона приема
│   ├── CreateVisitTemplateModal.tsx   # Создание шаблона приема
│   ├── CreateExamTextTemplateModal.tsx # Создание шаблона текста осмотра
│   ├── DeleteTemplateConfirmModal.tsx # Подтверждение удаления шаблона
│   ├── InformedConsentForm.tsx       # Форма информированного согласия
│   ├── VisitCard.tsx                  # Карточка приема в списке
│   ├── VisitTypeSelector.tsx          # Выбор типа приема
│   └── VisitTicket025_1.tsx           # Печатная форма 025/у-1
│
└── services/                           # Бизнес-логика и IPC вызовы
    ├── visitService.ts                # Основной сервис приемов
    ├── visitTemplateService.ts        # Сервис шаблонов приемов
    ├── medicationTemplateService.ts   # Сервис шаблонов назначений
    ├── examTextTemplateService.ts     # Сервис шаблонов текста осмотра
    ├── informedConsentService.ts      # Сервис информированного согласия
    └── medicationDoseCalcService.ts   # Расчет дозировок препаратов

electron/modules/visits/
├── service.cjs                        # Backend бизнес-логика
├── handlers.cjs                       # IPC handlers для приемов
├── template-service.cjs               # Backend логика шаблонов
├── template-handlers.cjs              # IPC handlers для шаблонов
└── anamnesis-formatter.cjs            # Форматирование анамнеза для AI
```

## 🎯 Основной функционал

### 1. Управление приемами

- **Создание нового приема**: Форма с полным набором полей согласно форме 025/у
- **Редактирование приема**: Изменение данных существующего приема
- **Просмотр истории**: Список всех приемов пациента с фильтрацией и поиском
- **Удаление приема**: Удаление с подтверждением

### 2. Типы приемов

Модуль поддерживает следующие типы приемов:

- **primary** - Первичный прием
- **followup** - Повторный прием
- **consultation** - Консультация
- **emergency** - Неотложный прием
- **urgent** - Срочный прием

Тип приема влияет на:
- Отображение разделов формы (например, анамнез жизни только для первичного приема и консультации)
- Логику работы с шаблонами
- Печатные формы

### 3. Структура данных приема

Прием включает следующие разделы:

#### Антропометрия
- Вес (кг) с автоматическим расчетом ИМТ
- Рост (см) с автоматическим расчетом ППТ (площадь поверхности тела)
- Категория ИМТ по возрасту

#### Анамнез жизни (форма 025/у)
**Отображается только для первичного приема и консультации**

- **Наследственность**: Туберкулез, диабет, гипертония, онкология, аллергии, прочее
- **Сведения о беременности и родах**:
  - Течение беременности
  - Акушерский анамнез
  - Способ родоразрешения (естественные/кесарево)
  - Длительность беременности (недель, формат: недели.дни, например 38.5)
  - Масса и рост при рождении
  - Оценка по шкале Апгар
  - Осложнения периода новорожденности
- **Вскармливание**:
  - Тип: Грудное / Искусственное / Смешанное
  - Длительность грудного вскармливания
  - Название молочной смеси (для искусственного/смешанного)
  - Возраст введения прикормов
  - Особенности питания
- **Перенесенные инфекционные заболевания**: Корь, ветрянка, краснуха, коклюш, скарлатина, ангина (с частотой), прочие
- **Аллергический статус**: Пищевая, лекарственная, на материалы, на укусы, сезонная аллергии

**Хранение**: Данные анамнеза жизни хранятся в JSON полях базы данных для структурированности и удобства обработки.

#### Анамнез заболевания
- Жалобы на момент поступления
- Когда началось заболевание и первые симптомы
- Течение болезни
- Лечение, проводимое до обращения

#### Показатели жизнедеятельности (Vital Signs)
- Артериальное давление (систолическое/диастолическое) с возрастными нормами
- Пульс (ЧСС) с возрастными нормами
- Частота дыхательных движений (ЧДД)
- Температура тела
- Сатурация кислорода (SpO2)
- Уровень сознания

#### Объективный осмотр
- Структурированный осмотр по системам (JSON)
- Текстовое описание общего состояния
- Шаблоны текста осмотра

#### Диагнозы
- **Основной диагноз**: Обязательное поле, связь с базой знаний заболеваний
- **Осложнения**: Множественный выбор
- **Сопутствующие диагнозы**: Множественный выбор

#### Назначения (Prescriptions)
- Список препаратов с дозировками
- Расчет дозировок на основе веса/возраста пациента
- Шаблоны назначений
- Маршрут введения, разведение, кратность приема

#### Информированное согласие
- Создание и привязка информированного согласия к приему

### 4. AI-интеграция (CDSS)

Модуль интегрирован с системой поддержки принятия клинических решений (CDSS):

#### Анализ симптомов
- Кнопка "Анализ AI" в разделе "Анамнез заболевания"
- Анализ жалоб и анамнеза с помощью Google Gemini API
- Парсинг симптомов из текста
- Семантический поиск в базе знаний заболеваний
- Ранжирование диагнозов по релевантности

#### Форматирование данных для AI
- `AnamnesisFormatter` преобразует структурированный анамнез жизни (JSON) в читаемый текст
- Включает все разделы: наследственность, перинатальный анамнез, вскармливание, инфекции, аллергии
- Учитывает клинически значимые данные (недоношенность, ранний прикорм, частые ангины и т.д.)

#### Рекомендации по терапии
- Автоматический подбор препаратов для выбранного диагноза
- Расчет дозировок на основе веса и возраста пациента
- Отображение рекомендаций с возможностью добавления в назначения

### 5. Шаблоны

#### Шаблоны приемов
- Создание шаблонов для быстрого заполнения повторяющихся данных
- Применение шаблона к новому приему
- Редактирование и удаление шаблонов
- При создании шаблона можно перезаписать существующий

#### Шаблоны назначений
- Создание наборов препаратов для частых диагнозов
- Пакетное добавление препаратов из шаблона
- Пакетное редактирование дозировок в шаблоне
- Создание шаблона из текущих назначений

#### Шаблоны текста осмотра
- Быстрое добавление стандартных формулировок объективного осмотра

### 6. Валидация

Модуль использует **двойную валидацию**:

1. **Frontend валидация** (`visitService.ts`):
   - Использует `VisitSchema` из `validators/visit.validator.ts`
   - Быстрая обратная связь для пользователя
   - Предотвращает отправку невалидных данных

2. **Backend валидация** (`handlers.cjs`):
   - Повторная валидация тех же схем
   - Защита от обхода frontend валидации
   - Безопасность данных

#### Схемы валидации

- `VisitSchema`: Основная схема приема
- `AnalyzeVisitSchema`: Схема для AI-анализа
- `HeredityDataSchema`, `BirthDataSchema`, `FeedingDataSchema`, и др.: Схемы для структурированных данных анамнеза

### 7. Печатные формы

- **Форма 025/у-1**: Печатная форма приема согласно приказу Минздрава
- Компонент `VisitTicket025_1.tsx` для генерации печатной формы

## 🔧 Компоненты

### Основные страницы

#### `VisitsModule.tsx`
- Отображает список всех приемов пациента
- Поиск по жалобам и диагнозам
- Статистика приемов
- Навигация к созданию нового приема

#### `VisitFormPage.tsx`
- Главная форма создания/редактирования приема
- Интегрирует все разделы приема
- Управление состоянием формы
- Обработка AI-анализа
- Сохранение и валидация

### Секции формы

#### `AnamnesisSection.tsx`
- Обертка для анамнеза жизни (форма 025/у)
- Условное отображение (только для первичного приема и консультации)
- Парсинг JSON данных из базы
- Интеграция всех подсекций анамнеза

#### `DiseaseHistorySection.tsx`
- Анамнез заболевания
- Поля: жалобы, начало заболевания, течение, лечение до обращения
- Кнопка "Анализ AI" для запуска CDSS

#### `VitalSignsSection.tsx`
- Показатели жизнедеятельности
- Валидация по возрастным нормам
- Визуальная индикация отклонений

#### `PhysicalExamBySystems.tsx`
- Структурированный объективный осмотр
- Разделы по системам организма
- Текстовое описание

### Выбор диагнозов

#### `DiagnosisSelector.tsx`
- Выбор основного диагноза
- Интеграция с базой знаний заболеваний
- Поиск по МКБ-10 кодам

#### `MultipleDiagnosisSelector.tsx`
- Множественный выбор осложнений и сопутствующих диагнозов
- Управление списком выбранных диагнозов

### Работа с препаратами

#### `MedicationBrowser.tsx`
- Браузер всех доступных препаратов
- Поиск и фильтрация
- Добавление в назначения

#### `MedicationDoseModal.tsx`
- Расчет дозировки препарата
- Учет веса, возраста, площади поверхности тела
- Выбор маршрута введения
- Расчет разведения

#### `MedicationSelector.tsx`
- Выбор препарата из списка
- Интеграция с расчетом дозировок

### Шаблоны

#### `VisitTemplateSelector.tsx`
- Выбор шаблона приема
- Применение шаблона к форме

#### `MedicationTemplateSelector.tsx`
- Выбор шаблона назначений
- Пакетное добавление препаратов

## 🔌 Сервисы

### `visitService.ts`

Основной сервис для работы с приемами:

```typescript
visitService.getVisits(childId)              // Получить все приемы пациента
visitService.getVisit(id)                     // Получить прием по ID
visitService.upsertVisit(data)                // Создать/обновить прием
visitService.deleteVisit(id)                  // Удалить прием
visitService.analyzeVisit(visitId)           // AI-анализ симптомов
visitService.getMedicationsForDiagnosis(...)  // Получить препараты для диагноза
visitService.checkDuplicateMedication(...)    // Проверка дубликатов препаратов
```

### `visitTemplateService.ts`

Работа с шаблонами приемов:

```typescript
visitTemplateService.getTemplates()           // Получить все шаблоны
visitTemplateService.createTemplate(data)      // Создать шаблон
visitTemplateService.updateTemplate(id, data) // Обновить шаблон
visitTemplateService.deleteTemplate(id)      // Удалить шаблон
visitTemplateService.applyTemplate(id, visit) // Применить шаблон к приему
```

### `medicationTemplateService.ts`

Работа с шаблонами назначений:

```typescript
medicationTemplateService.getTemplates()       // Получить все шаблоны
medicationTemplateService.createTemplate(...)  // Создать шаблон
medicationTemplateService.updateTemplate(...)  // Обновить шаблон
medicationTemplateService.deleteTemplate(...)  // Удалить шаблон
```

### `informedConsentService.ts`

Работа с информированным согласием:

```typescript
informedConsentService.getById(id)            // Получить согласие по ID
informedConsentService.create(data)           // Создать согласие
informedConsentService.update(id, data)       // Обновить согласие
```

## 🗄️ База данных

### Модель Visit (Prisma)

Основные поля:
- `id`, `childId`, `doctorId`
- `visitDate`, `visitTime`, `visitType`, `visitPlace`
- `complaints`, `diseaseOnset`, `diseaseCourse`, `treatmentBeforeVisit`
- `currentWeight`, `currentHeight`, `bmi`, `bsa`
- `heredityData` (JSON), `birthData` (JSON), `feedingData` (JSON), `infectiousDiseasesData` (JSON), `allergyStatusData` (JSON)
- `vitalSigns` (JSON)
- `physicalExam` (JSON)
- `primaryDiagnosis` (JSON), `complications` (JSON), `comorbidities` (JSON)
- `prescriptions` (JSON)
- `status`, `informedConsentId`
- `createdAt`, `updatedAt`

### JSON поля

Структурированные данные хранятся в JSON полях для:
- Гибкости структуры
- Удобства печати
- Обработки AI
- Сохранения иерархии данных

## 🔐 Безопасность

1. **Аутентификация**: Все IPC handlers защищены `ensureAuthenticated`
2. **Валидация**: Двойная валидация (frontend + backend)
3. **Логирование**: Все ошибки логируются через централизованный `logger`
4. **Транзакции**: Критические операции выполняются в транзакциях Prisma

## 📊 Интеграции

### База знаний заболеваний
- Поиск заболеваний по названию, МКБ-10 коду
- Получение информации о заболевании (симптомы, диагностика, лечение)
- Подбор препаратов для диагноза

### AI (Google Gemini)
- Анализ жалоб и анамнеза
- Парсинг симптомов
- Семантический поиск диагнозов
- Ранжирование по релевантности

### Расчет дозировок
- Учет веса пациента
- Учет возраста
- Учет площади поверхности тела (BSA)
- Различные формулы расчета

## 🎨 UI/UX особенности

- **Адаптивный дизайн**: Работает на разных размерах экрана
- **Темная тема**: Поддержка dark mode
- **Валидация в реальном времени**: Быстрая обратная связь
- **Плавающие карточки**: AI рекомендации и выбранный диагноз остаются видимыми при скролле
- **Модальные окна**: Для выбора диагнозов, препаратов, шаблонов
- **Side panels**: Для просмотра информации о заболеваниях
- **Автозаполнение**: Загрузка данных из предыдущих приемов

## 📝 Примечания для разработчиков

1. **Всегда используйте сервисы** для бизнес-логики, не пишите её в компонентах
2. **Валидируйте данные дважды**: в сервисе и в IPC handler
3. **Логируйте ошибки** через `logger`, не используйте `console.log`
4. **Используйте транзакции** для связанных операций
5. **Парсите JSON поля** только на границе с базой данных (в backend service)
6. **Следуйте архитектуре слоев**: Component → Service → IPC → Database

## 🔄 Миграции

При изменении структуры данных приема:
1. Обновите `prisma/schema.prisma`
2. Создайте миграцию: `npx prisma migrate dev --name description`
3. Обновите TypeScript типы в `src/types.ts`
4. Обновите Zod схемы в `validators/`
5. Обновите компоненты и сервисы
6. Обеспечьте обратную совместимость при изменении JSON полей

## 📚 Дополнительные ресурсы

- `AI_CODING_GUIDELINES.md` - Общие принципы разработки
- `DEVELOPMENT_RULES.md` - Правила разработки
- `AI_INTEGRATION_ANALYSIS.md` - Детальный анализ AI-интеграции
- Форма 025/у - Официальная форма Минздрава РФ
