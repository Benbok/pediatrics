# Модуль Diseases (Заболевания)

## 📋 Общее описание

Модуль `diseases` отвечает за управление базой знаний заболеваний в педиатрической системе. Модуль реализует полный цикл работы с заболеваниями: создание, редактирование, загрузка клинических рекомендаций (PDF), поиск по симптомам, связь с препаратами, заметки врачей, а также предоставляет структурированные планы диагностики и лечения.

## 🏗️ Архитектура

Модуль следует строгой многослойной архитектуре согласно `AI_CODING_GUIDELINES.md`:

```
┌─────────────────────────────────────────────────────────┐
│  Component Layer (*.tsx)                                │
│  - UI только, NO business logic                          │
│  - Обработка пользовательского ввода                    │
│  - Отображение данных                                    │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ↓
┌─────────────────────────────────────────────────────────┐
│  Service Layer (*.service.ts)                           │
│  - ВСЯ бизнес-логика                                     │
│  - Zod валидация (первый уровень)                       │
│  - IPC вызовы к backend                                  │
│  - Обработка ошибок и логирование                        │
│  - Инвалидация кеша через dataEvents                    │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ↓
┌─────────────────────────────────────────────────────────┐
│  IPC Handler Layer (electron/modules/diseases/*.cjs)   │
│  - ensureAuthenticated middleware                        │
│  - Zod валидация (второй уровень - безопасность)        │
│  - Простые DB операции через Prisma                     │
│  - Парсинг PDF клинических рекомендаций                 │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ↓
┌─────────────────────────────────────────────────────────┐
│  Database Layer (Prisma)                                 │
│  - Хранение данных                                       │
│  - Миграции                                              │
│  - Связи с препаратами (DiseaseMedication)             │
│  - Клинические рекомендации (ClinicalGuideline)         │
│  - Заметки врачей (DiseaseNote)                         │
└─────────────────────────────────────────────────────────┘
```

### Принципы архитектуры

1. **Разделение ответственности**: Каждый слой имеет четко определенную роль
2. **Двойная валидация**: Frontend (UX) + Backend (Security)
3. **Централизованное логирование**: Все ошибки через `logger`
4. **Аутентификация**: Все IPC handlers защищены `ensureAuthenticated`
5. **Кеширование**: Автоматическая инвалидация кеша при изменениях через `dataEvents`
6. **PDF-парсинг**: Автоматическое извлечение структурированных данных из клинических рекомендаций

## 📁 Структура файлов

```
src/modules/diseases/
├── README.md                          # Этот файл
├── DiseasesModule.tsx                 # Список всех заболеваний
├── DiseaseFormPage.tsx               # Форма создания/редактирования заболевания
├── DiseaseDetailPage.tsx             # Детальная информация о заболевании
│
├── components/                        # UI компоненты
│   ├── DiseaseCard.tsx               # Карточка заболевания в списке
│   ├── DiseaseKnowledgeView.tsx      # Просмотр базы знаний заболевания
│   ├── DiseaseMedicationsTab.tsx     # Вкладка с препаратами для заболевания
│   ├── DiseaseNoteCard.tsx           # Карточка заметки
│   ├── DiseaseNoteEditor.tsx         # Редактор заметок
│   ├── DiseaseNotesList.tsx          # Список заметок к заболеванию
│   └── GuidelinesList.tsx            # Список клинических рекомендаций
│
├── services/                          # Бизнес-логика и IPC вызовы
│   └── diseaseService.ts             # Основной сервис заболеваний
│
└── templates/                         # Шаблоны
    └── diseaseJsonTemplate.json      # Шаблон JSON для импорта

electron/modules/diseases/
├── service.cjs                       # Backend бизнес-логика
├── handlers.cjs                      # IPC handlers для заболеваний
└── validator.cjs                     # Валидация заболеваний
```

## 🎯 Основной функционал

### 1. Управление заболеваниями

- **Создание нового заболевания**: Форма с полным набором полей
- **Редактирование заболевания**: Изменение данных существующего заболевания
- **Просмотр базы**: Список всех заболеваний с фильтрацией и поиском
- **Удаление заболевания**: Удаление с подтверждением
- **Импорт из JSON**: Импорт структурированных данных о заболевании

### 2. Структура данных заболевания

#### Основная информация
- **Название (RU)**: Обязательное поле
- **Название (EN)**: Опциональное
- **Описание**: Текстовое описание заболевания
- **ICD-10 код**: Основной код МКБ-10 (обязательное)
- **ICD-10 коды**: Массив всех связанных кодов МКБ-10

#### Клиническая информация
- **Симптомы** (`symptoms`): Массив симптомов заболевания
- **Дифференциальная диагностика** (`differentialDiagnosis`): Массив заболеваний для дифференциации
- **Красные флаги** (`redFlags`): Массив тревожных признаков

#### Структурированные планы

**План диагностики** (`diagnosticPlan`):
Массив объектов `DiagnosticPlanItem`:
- `type`: Тип исследования (`lab` | `instrumental`)
- `test`: Название исследования
- `priority`: Приоритет (`low` | `medium` | `high`)
- `rationale`: Обоснование

**План лечения** (`treatmentPlan`):
Массив объектов `TreatmentPlanItem`:
- `category`: Категория лечения (`symptomatic` | `etiologic` | `supportive` | `other`)
- `description`: Описание лечения
- `priority`: Приоритет (`low` | `medium` | `high`)

### 3. Клинические рекомендации (Guidelines)

Модуль поддерживает загрузку и работу с клиническими рекомендациями в формате PDF:

#### Загрузка рекомендаций
- **Одиночная загрузка**: Загрузка одного PDF файла
- **Пакетная загрузка**: Загрузка нескольких PDF файлов одновременно
- **Прогресс загрузки**: Отслеживание прогресса парсинга каждого файла
- **Автоматический парсинг**: Извлечение структурированных данных из PDF

#### Индексация PDF для поиска (GuidelineChunk + FTS5)

Для улучшения диагностического поиска и снижения зависимости от AI, PDF дополнительно индексируются в нормализованной таблице `GuidelineChunk`.

- **`ClinicalGuideline.chunks`**: сохраняется legacy JSON-нарезка (обратная совместимость для UI/старого поиска)
- **`GuidelineChunk`**: нормализованные чанки с метаданными
  - `guidelineId`, `diseaseId`
  - `type` (клиническая категория: `complaints`, `physicalExam`, `treatment`, ...)
  - `pageStart/pageEnd`, `sectionTitle`
  - `text`
  - `embeddingJson` (опционально)

##### FTS5: offline-first, без триггеров

Полнотекстовый поиск по `GuidelineChunk.text` делается через SQLite FTS5 виртуальную таблицу `guideline_chunks_fts`.

Важный архитектурный принцип: **FTS не синхронизируется SQLite-триггерами**. Вместо этого используется программная пересборка индекса:

- `electron/services/chunkIndexService.cjs` → `ChunkIndexService.rebuildFts()`
  - `DROP TRIGGER IF EXISTS ...` (на случай старого состояния)
  - `DROP TABLE IF EXISTS guideline_chunks_fts`
  - `CREATE VIRTUAL TABLE guideline_chunks_fts USING fts5(...)`
  - `INSERT INTO guideline_chunks_fts SELECT ... FROM guideline_chunks`

Эта стратегия устраняет классы ошибок SQLite вида `SQL logic error` при удалениях/обновлениях и гарантирует согласованность FTS с основной таблицей.

#### Структурированные секции
После парсинга PDF извлекаются следующие секции:
- `definition`: Определение заболевания
- `etiology`: Этиология
- `epidemiology`: Эпидемиология
- `classification`: Классификация
- `clinicalPicture`: Клиническая картина
- `complaints`: Жалобы
- `physicalExam`: Физикальное обследование
- `labDiagnostics`: Лабораторная диагностика
- `instrumental`: Инструментальная диагностика
- `treatment`: Лечение
- `rehabilitation`: Реабилитация
- `prevention`: Профилактика
- `medications`: Препараты (JSON массив)

#### Поиск в рекомендациях
- **Полнотекстовый поиск**: Поиск по содержимому всех загруженных PDF
- **Поиск в конкретном файле**: Фильтрация по выбранному файлу
- **Выделение результатов**: Подсветка найденных фрагментов
- **Навигация по результатам**: Переход к найденным фрагментам

#### Управление рекомендациями
- Переименование файлов
- Удаление рекомендаций
- Просмотр содержимого
- Скачивание PDF файлов

При изменении данных чанков (upload/delete/reindex) выполняется `ChunkIndexService.rebuildFts()`.

### 4. GuidelinePlan API

Модуль предоставляет нормализованный API для получения структурированных планов диагностики и лечения:

#### Источники данных (приоритет)
1. **Структурированные данные заболевания** (`disease_structured`): Данные, введенные вручную в форму
2. **Сырые данные из рекомендаций** (`guideline_raw`): Извлеченные из PDF, требуют проверки
3. **Отсутствие данных** (`none`): Планы не заполнены

#### Структура GuidelinePlan
```typescript
{
  diseaseId: number;
  diagnosticPlan: DiagnosticPlanItem[];
  treatmentPlan: TreatmentPlanItem[];
  differentialDiagnosis: string[];
  redFlags: string[];
  source: 'disease_structured' | 'guideline_raw' | 'none';
  needsReview: boolean;
  raw: {
    labDiagnostics?: string;
    instrumental?: string;
    treatment?: string;
    medications?: string;
  } | null;
}
```

#### Fallback механизм
Если структурированные планы отсутствуют, система автоматически создает планы из сырых данных рекомендаций с флагом `needsReview: true`.

### 5. Заметки врачей (Disease Notes)

Модуль поддерживает систему заметок к заболеваниям:

#### Типы заметок
- **Личные заметки**: Видны только автору
- **Общие заметки**: Видны всем пользователям (`isShared: true`)
- **Закрепленные заметки**: Отображаются вверху списка (`isPinned: true`)

#### Функционал заметок
- Создание, редактирование, удаление
- Теги для организации
- Markdown поддержка
- История изменений
- Поиск по заметкам

### 6. Связь с препаратами

- **Автоматический подбор**: Препараты подбираются по ICD-10 кодам
- **Приоритизация**: Назначение приоритета препарату для заболевания
- **Группировка**: Группировка препаратов по фармакологическим группам
- **Специфическое дозирование**: Индивидуальные правила дозирования для конкретного заболевания

### 7. Поиск по симптомам

Модуль предоставляет семантический поиск заболеваний по симптомам:

- **Входные данные**: Массив симптомов
- **Алгоритм**: Семантический поиск в базе знаний
- **Результат**: Ранжированный список заболеваний по релевантности
- **Использование**: Интеграция с AI-анализом симптомов в модуле приемов

### 8. Импорт из JSON

- Вставка JSON-структуры данных о заболевании
- Валидация структуры
- Проверка обязательных полей
- Предпросмотр перед сохранением
- Предупреждения о недостающих данных

### 9. Валидация

Модуль использует **двойную валидацию**:

1. **Frontend валидация** (`diseaseService.ts`):
   - Использует `DiseaseSchema` из `validators/disease.validator.ts`
   - Быстрая обратная связь для пользователя
   - Предотвращает отправку невалидных данных

2. **Backend валидация** (`handlers.cjs`):
   - Повторная валидация тех же схем
   - Защита от обхода frontend валидации
   - Безопасность данных

#### Схемы валидации

- `DiseaseSchema`: Основная схема заболевания
- `DiagnosticPlanItemSchema`: Схема элемента плана диагностики
- `TreatmentPlanItemSchema`: Схема элемента плана лечения

## 🔧 Компоненты

### `DiseasesModule.tsx`

Главная страница базы заболеваний:

- **Отображение списка**: Все заболевания с карточками
- **Поиск**: Поиск по названию, ICD-10 коду, симптомам
- **Навигация**: 
  - Переход к деталям заболевания
  - Создание нового заболевания
- **Удаление**: Удаление с подтверждением
- **Статистика**: Общее количество заболеваний

**Особенности**:
- Кеширование данных через `DataCacheContext`
- Оптимистичное обновление UI при удалении
- Отображение ICD-10 кодов и симптомов

### `DiseaseFormPage.tsx`

Форма создания/редактирования заболевания:

- **Основная информация**: Название, ICD-10 код, описание
- **Симптомы**: Динамическое добавление симптомов
- **План диагностики**: Структурированные элементы диагностики
- **План лечения**: Структурированные элементы лечения
- **Дифференциальная диагностика**: Список заболеваний для дифференциации
- **Красные флаги**: Тревожные признаки
- **Импорт из JSON**: Импорт структурированных данных

**Особенности**:
- Валидация ICD-10 кодов
- Динамическое управление списками
- Предпросмотр импортированных данных

### `DiseaseDetailPage.tsx`

Детальная страница заболевания:

- **Информация о заболевании**: Полная карточка с данными
- **База знаний**: Просмотр всех данных через `DiseaseKnowledgeView`
- **Редактирование**: Переход к форме редактирования
- **Удаление**: Удаление с подтверждением

### `DiseaseKnowledgeView.tsx`

Комплексный просмотр базы знаний заболевания:

- **Вкладки**: Организация информации по вкладкам
  - Описание и симптомы
  - План диагностики
  - План лечения
  - Дифференциальная диагностика
  - Красные флаги
  - Клинические рекомендации
  - Препараты
  - Заметки
- **Поиск в PDF**: Полнотекстовый поиск по рекомендациям
- **Навигация**: Быстрый доступ ко всем разделам

### `DiseaseCard.tsx`

Карточка заболевания в списке:

- Название заболевания
- ICD-10 код
- Основные симптомы
- Переход к деталям

### `GuidelinesList.tsx`

Список клинических рекомендаций:

- Отображение всех загруженных PDF
- Загрузка новых файлов (одиночная и пакетная)
- Отслеживание прогресса парсинга
- Переименование файлов
- Удаление рекомендаций
- Скачивание PDF
- Просмотр содержимого

### `DiseaseMedicationsTab.tsx`

Вкладка с препаратами для заболевания:

- Список препаратов, связанных с заболеванием
- Группировка по фармакологическим группам
- Приоритет препаратов
- Переход к деталям препарата

### `DiseaseNotesList.tsx`

Список заметок к заболеванию:

- Отображение всех заметок
- Фильтрация (личные/общие, закрепленные)
- Поиск по заметкам
- Создание новых заметок
- Редактирование и удаление

### `DiseaseNoteEditor.tsx`

Редактор заметок:

- Создание и редактирование заметок
- Поддержка Markdown
- Теги
- Настройки видимости (личная/общая)
- Закрепление заметки

## 🔌 Сервисы

### `diseaseService.ts`

Основной сервис для работы с заболеваниями:

```typescript
diseaseService.getDiseases()                    // Получить все заболевания
diseaseService.getDisease(id)                  // Получить заболевание по ID
diseaseService.upsertDisease(data)            // Создать/обновить заболевание
diseaseService.deleteDisease(id)               // Удалить заболевание
diseaseService.searchBySymptoms(symptoms)     // Поиск по симптомам
diseaseService.getGuidelinePlan(diseaseId)     // Получить нормализованный план
diseaseService.uploadGuideline(diseaseId, pdfPath) // Загрузить рекомендацию
diseaseService.uploadGuidelinesBatch(...)     // Пакетная загрузка рекомендаций
diseaseService.uploadGuidelinesAsync(...)      // Асинхронная очередь загрузки
diseaseService.updateGuideline(id, title)     // Обновить рекомендацию
diseaseService.deleteGuideline(id)            // Удалить рекомендацию
diseaseService.reindexGuidelineChunks()        // Пересобрать GuidelineChunk + FTS
diseaseService.importFromJson(jsonString)     // Импорт из JSON
```

**Особенности сервиса**:
- Автоматическая нормализация JSON полей
- Автоматическая инвалидация кеша через `dataEvents`
- Централизованное логирование ошибок
- Безопасный парсинг JSON полей

## 🗄️ База данных

### Модель Disease (Prisma)

Основные поля:
- `id`, `icd10Code`, `icd10Codes` (JSON), `nameRu`, `nameEn`
- `description`
- `symptoms` (JSON)
- `diagnosticPlan` (JSON)
- `treatmentPlan` (JSON)
- `differentialDiagnosis` (JSON)
- `redFlags` (JSON)
- `createdAt`, `updatedAt`

### Модель ClinicalGuideline (Prisma)

Основные поля:
- `id`, `diseaseId`, `title`, `pdfPath`
- `content`: Полный текст PDF
- `chunks`: JSON массив фрагментов для поиска
- `source`: Источник рекомендации
- Структурированные секции: `definition`, `etiology`, `epidemiology`, и т.д.
- `medications`: JSON массив препаратов
- `createdAt`

### Модель DiseaseNote (Prisma)

Основные поля:
- `id`, `diseaseId`, `authorId`
- `title`, `content`
- `tags` (JSON)
- `isPinned`, `isShared`
- `createdAt`, `updatedAt`

### JSON поля

Структурированные данные хранятся в JSON полях для:
- Гибкости структуры
- Удобства обработки
- Сохранения иерархии данных
- Поддержки сложных структур (массивы объектов)

### Связи

- **DiseaseMedication**: Связь многие-ко-многим с препаратами
- **ClinicalGuideline**: Один-ко-многим (одно заболевание - много рекомендаций)
- **DiseaseNote**: Один-ко-многим (одно заболевание - много заметок)

## 🔐 Безопасность

1. **Аутентификация**: Все IPC handlers защищены `ensureAuthenticated`
2. **Валидация**: Двойная валидация (frontend + backend)
3. **Логирование**: Все ошибки логируются через централизованный `logger`
4. **Audit Trail**: Полная история изменений через `logAudit`
5. **Разделение доступа**: Заметки могут быть личными или общими

## 📊 Интеграции

### AI (Google Gemini)
- Парсинг PDF клинических рекомендаций
- Извлечение структурированных данных из PDF
- Семантический поиск по симптомам

AI является **опциональным усилителем**:
- `embeddingJson` для `GuidelineChunk` вычисляется только при наличии ключа
- при ошибках квоты/geo API система деградирует к **FTS5 + симптомному поиску**

### Модуль препаратов (Medications)
- Связь препаратов с заболеваниями по ICD-10
- Автоматический подбор препаратов для диагноза
- Приоритизация препаратов

### Модуль приемов (Visits)
- Использование базы знаний для AI-анализа симптомов
- Подбор диагнозов на основе симптомов
- Использование планов диагностики и лечения

### Система кеширования
- Автоматическая инвалидация кеша при изменениях
- Оптимизация производительности
- Namespace-based кеширование (`diseases`)

## 🎨 UI/UX особенности

- **Адаптивный дизайн**: Работает на разных размерах экрана
- **Темная тема**: Поддержка dark mode
- **Валидация в реальном времени**: Быстрая обратная связь
- **Поиск**: Мгновенный поиск по названию, коду, симптомам
- **Вкладки**: Организация информации по вкладкам
- **PDF просмотр**: Встроенный просмотр клинических рекомендаций
- **Поиск в PDF**: Полнотекстовый поиск по содержимому
- **Заметки**: Удобная система заметок с Markdown
- **Оптимистичное обновление**: Мгновенная обратная связь при удалении

## 📝 Примечания для разработчиков

1. **Всегда используйте сервисы** для бизнес-логики, не пишите её в компонентах
2. **Валидируйте данные дважды**: в сервисе и в IPC handler
3. **Логируйте ошибки** через `logger`, не используйте `console.log`
4. **Используйте dataEvents** для инвалидации кеша при изменениях
5. **Следуйте архитектуре слоев**: Component → Service → IPC → Database
6. **Парсите JSON поля** только на границе с базой данных (в backend service)
7. **Используйте GuidelinePlan API**: Для получения нормализованных планов диагностики и лечения
8. **Нормализуйте данные**: Используйте `normalizeDisease` перед использованием данных

## 🔄 Миграции

При изменении структуры данных заболевания:
1. Обновите `prisma/schema.prisma`
2. Создайте миграцию: `npx prisma migrate dev --name description`
3. Обновите TypeScript типы в `src/types.ts`
4. Обновите Zod схемы в `validators/disease.validator.ts`
5. Обновите компоненты и сервисы
6. Обеспечьте обратную совместимость при изменении JSON полей
7. Обновите шаблон импорта из JSON при необходимости

## 🔗 Связанные модули

- **Visits Module** (`src/modules/visits/`) - Использование базы знаний для AI-анализа
- **Medications Module** (`src/modules/medications/`) - Связь препаратов с заболеваниями
- **Services** (`src/services/disease.service.ts`) - Бизнес-логика

## 📚 Дополнительные ресурсы

- `AI_CODING_GUIDELINES.md` - Общие принципы разработки
- `DEVELOPMENT_RULES.md` - Правила разработки
- `src/validators/disease.validator.ts` - Схемы валидации
- `templates/diseaseJsonTemplate.json` - Шаблон для импорта из JSON
- `src/utils/diseaseNormalization.cjs` - Нормализация данных заболеваний
